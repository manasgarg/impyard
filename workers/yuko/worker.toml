# Worker spec — OWNER-ONLY. Overlays org.toml at scope "org/yuko".
name = "yuko"

# This worker's own cap (scope "org/yuko"). Grants are inherited from org.toml.
[[budget.limit]]
currency = "model_calls"
window = "day"
max = 5000
